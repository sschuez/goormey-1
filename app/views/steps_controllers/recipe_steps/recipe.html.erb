<%= render :layout => 'form' do |f| %>
  
  <%= render "recipes/recipe_form", f: f %>
  
  <div class="form-actions">
    <%= f.submit "ingredients", class: "btn btn-secondary" %>
  </div>
  
<% end %>