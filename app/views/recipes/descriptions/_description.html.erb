<%= turbo_frame_tag nested_dom_id(:description, recipe) do %>

  <% if recipe.name.blank? %>
    <%= render "recipes/descriptions/empty_state", attribute: "title" %>
  <% else %>
    <h4><%= recipe.name %></h4>
  <% end %>
        
  <% if recipe.description.blank? %>
    <%= render "recipes/descriptions/empty_state", attribute: "description" %>
  <% else %>
    <p><%= recipe.description %></p>
  <% end %>
        
  <%# EDIT DESCRIPTION BUTTON %>
  <% if policy(recipe).update? %>
    <div class="flex-end">
      <%= link_to edit_recipe_description_path(recipe), 
        class: "btn btn-ghost btn-coral",
        data: { turbo_frame: nested_dom_id(:description, recipe) } do %>
        <%= Icon.call("edit") %>
        description
      <% end %>
    </div>
  <% end %>

<% end %>
    