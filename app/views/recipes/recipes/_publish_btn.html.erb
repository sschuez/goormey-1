<%= turbo_frame_tag "recipe_#{recipe.id}_publish_btn_#{location}" do %>
  <% if policy(recipe).update? %>
    
    <%= button_to publish_recipe_path(recipe), class: "btn-styling", method: :patch do %>
      <%= recipe.published_icon %>
    <% end %>

  <% end %>
<% end %>